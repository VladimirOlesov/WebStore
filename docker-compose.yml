version: '3.9'
services:
  postgres:
    image: postgres:16
    container_name: web_store_test_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: web_store_test_db
    volumes:
      - /c/Users/Vladimir/postgres:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: default@defaultmail.com
      PGADMIN_DEFAULT_PASSWORD: password
    ports:
      - "5050:80"